<div .row>
  <div .col-lg-12 .col-md-12 .col-sm-12 .col-xs-12 .story_cards>
    $forall msg <- msgs
      <div>
        #{msg}
    $maybe _ <- mr
      <div .card>
        <div .card-inner>
          Updated criterion
    $nothing 
    <div .card>
      <div .card-inner>
        <form role=form method=post enctype=#{enctype} action=@{AdminEditCriterionR criterionId}>
          ^{widget}
          <button type="submit" .btn .btn-default>
            Submit
