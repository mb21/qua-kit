$if null storedFiles
    <p>No files have been uploaded yet.
$else
    <h2>Previously submitted files
    <ul>
        $forall (Entity ident (ustory)) <- storedFiles
          <li>
            <table>
              <tr>
                <td>
                  <img src="@{DownloadR ident}" style="height: 100px;"><br>
                  #{storyCountry ustory} - #{storyCity ustory}
               <td>
                 #{show $ storyAuthor ustory}<br>
                 #{show $ storyCourse ustory}
               <td>
                 <pre>#{storyComment ustory}
